# Compiled artifacts (built by `make`)
/macthermal
/macthermal-gui
/MacThermal.app/

# Test runner (built by `make test`)
/.macthermal-tests

# Build / object artifacts
*.o
*.swiftmodule
.build/

# SwiftPM / Xcode local state (Package.swift is committed; these aren't)
.swiftpm/
Package.resolved
*.xcodeproj

# macOS
.DS_Store
