See [AGENTS.md](AGENTS.md) for all repo guidance — build/test/run commands,
source layout, architecture rules, and the non-obvious gotchas (the 80-byte
SMC struct ABI, `%@` padding, key caching, the `thermal` naming collision).

That file is the single source of truth; keep it updated rather than this one.
